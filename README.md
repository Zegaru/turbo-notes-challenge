# notes-challenge

Monorepo: Django + DRF backend, Next.js + TypeScript frontend.

## Setup

```bash
cp .env.example .env
docker compose up
```

## Environment Variables

<!-- TODO: Document required env vars (e.g. DJANGO_SECRET_KEY, POSTGRES_*, CORS_ORIGINS) -->
